name: Auto-merge clean PRs

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

permissions:
  contents: write
  pull-requests: write
  actions: write

concurrency:
  group: "automerge-${{ github.event.pull_request.number }}"
  cancel-in-progress: true

jobs:
  automerge:
    if: "!github.event.pull_request.draft"
    runs-on: ubuntu-latest

    steps:
      # 📥 Ré-installe un repo Git minimal pour que `gh pr merge` trouve .git
      - name: Checkout minimal
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Tenter l’auto-merge (ou mise en file d’attente)
        env:
          # WORKFLOW_TOKEN (PAT) permet de déclencher d'autres workflows via gh workflow run.
          # GITHUB_TOKEN peut être insuffisant selon la configuration GitHub.
          GH_TOKEN: ${{ secrets.WORKFLOW_TOKEN || github.token }}
        run: |
          set -eo pipefail
          PR="${{ github.event.pull_request.number }}"

          # --auto : active l’auto-merge ou place la PR en merge-queue
          if ! gh pr merge "$PR" --squash --delete-branch --auto; then
            gh pr comment "$PR" \
              --body ":warning: **Conflit détecté** – merci de le résoudre manuellement."
            exit 0
          fi

      - name: Attendre le merge effectif
        env:
          GH_TOKEN: ${{ secrets.WORKFLOW_TOKEN || github.token }}
        run: |
          set -euo pipefail
          PR="${{ github.event.pull_request.number }}"
          MAX_ATTEMPTS=30
          SLEEP_SECONDS=10
          MERGED_AT=""

          for attempt in $(seq 1 "$MAX_ATTEMPTS"); do
            MERGED_AT=$(gh pr view "$PR" --json mergedAt --jq '.mergedAt')
            if [ -n "$MERGED_AT" ] && [ "$MERGED_AT" != "null" ]; then
              echo "PR merged at $MERGED_AT."
              break
            fi
            echo "PR not merged yet (attempt ${attempt}/${MAX_ATTEMPTS})."
            sleep "$SLEEP_SECONDS"
          done

          if [ -z "$MERGED_AT" ] || [ "$MERGED_AT" = "null" ]; then
            echo "PR not merged after waiting. Skipping release pipeline."
            exit 0
          fi

      - name: Déclencher le pipeline release
        env:
          GH_TOKEN: ${{ secrets.WORKFLOW_TOKEN || github.token }}
        run: |
          set -euo pipefail
          gh workflow run "Build deploy branch" --ref main
          gh workflow run "Build plugin zip" --ref main
